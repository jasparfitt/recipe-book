# Coook Mobile App
Built with React Native and Expo.

## Development
To start make sure you have the eas cli setup then run `npm run build-dev` once that has finished building download and install the dev version of the app. Then run `npm start` to start the development server.

## Build

